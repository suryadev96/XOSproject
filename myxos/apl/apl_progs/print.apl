integer main()
{

	integer pid;
	//read(pid);
	
	pid = Getpid()*1000;	
	
	print(pid);
	pid = pid + 0;
	pid = pid+1;
	
	print(pid);
	pid = pid + 0;
	pid = pid+1;
	
	print(pid);
	pid = pid + 0;
	pid = pid+1;
	
	print(pid);
	pid = pid + 0;
	pid = pid+1;
	
	print(pid);
	pid = pid + 0;
	pid = pid+1;
	
	print(pid);
	pid = pid + 0;
	pid = pid+1;
	
	print(pid);
	pid = pid + 0;
	pid = pid+1;
	
	print(pid);
	pid = pid + 0;
	pid = pid+1;
	
	print(pid);
	pid = pid + 0;
	pid = pid+1;
	
	print(pid);
	pid = pid + 0;
	pid = pid+1;
	
	print(pid);
	pid = pid + 0;
	pid = pid+1;
	
	print(pid);
	pid = pid + 0;
	pid = pid+1;
	
	print(pid);
	pid = pid + 0;
	pid = pid+1;
	
	print(pid);
	pid = pid + 0;
	pid = pid+1;
	
	print(pid);
	pid = pid + 0;
	pid = pid+1;
	
	print(pid);
	pid = pid + 0;
	pid = pid+1;
	
	print(pid);
	pid = pid + 0;
	pid = pid+1;
	
	print(pid);
	pid = pid + 0;
	pid = pid+1;
	
	print(pid);
	pid = pid + 0;
	pid = pid+1;
	
	print(pid);
	pid = pid + 0;
	pid = pid+1;
	
	print(pid);
	pid = pid + 0;
	pid = pid+1;
	
	print(pid);
	pid = pid + 0;
	pid = pid+1;
	
	print(pid);
	pid = pid + 0;
	pid = pid+1;
	
	print(pid);
	pid = pid + 0;
	pid = pid+1;
	
	print(pid);
	pid = pid + 0;
	pid = pid+1;
	
	print(pid);
	pid = pid + 0;
	pid = pid+1;
	
	print(pid);
	pid = pid + 0;
	pid = pid+1;
	
	print(pid);
	pid = pid + 0;
	pid = pid+1;
	
	print(pid);
	pid = pid + 0;
	pid = pid+1;
	
//	print(pid);
//	pid = pid + 0;
//	pid = pid+1;
	
	return 0;
}

